 Students: public List<Student> getRefreshTable() {
        String s = "SELECT DISTINCT s FROM Student s ";
        s += "ORDER BY s.dateCreated ASC";
        TypedQuery<Student> query = em.createQuery(s, Student.class);
        final List<Student> students = query.getResultList();
        return students;
    }
StudentView:
private List<Student> refreshtable;
refreshtable = students.getRefreshTable();

 public List<Student> getRefreshTable() {
        return refreshtable;
    }

    public void setRefreshTable(List<Student> refreshtable) {
        this.refreshtable = refreshtable;
    }

<p:commandButton value="Non-Ajax Submit" id="nonAjax" actionListener="#{buttonView.buttonAction}" ajax="false" />